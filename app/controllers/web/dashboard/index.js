const clients = require('./clients');
const programs = require('./programs');
const targets = require('./targets');
const datas = require('./datas');

module.exports = { clients, programs, targets, datas };
